# Lifecycle cheatsheet

DEFINE → PLAN → BUILD → VERIFY → REVIEW → SHIP

Commands: /spec /plan /build /test /review /ship /webperf /code-simplify /constraints

Build auto = approve plan once → sequential TDD tasks → pause on fail

Five-axis review: correctness | readability | architecture | security | performance

Lab: bilingual README, attribution, package auto, Pages public-only on Free
